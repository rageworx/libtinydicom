#pragma once
#ifndef __SWAP_H__
#define __SWAP_H__

namespace DicomImageViewer
{
    uint16_t SwapWORD(uint16_t nWord);
    uint32_t SwapDWORD(uint32_t nDWord);
}

#endif /// of __SWAP_H__
